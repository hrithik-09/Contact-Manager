<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape>
            <!-- Rounded corners -->
            <corners android:radius="20dp" />

            <!-- Background gradient -->
            <gradient
                android:startColor="#000000"
                android:endColor="#212121"
                android:angle="45" />
            <!--            <size android:height="115dp"/>-->
            <!-- Stroke with a gradient effect workaround -->
            <stroke android:width="1dp" android:color="#FF434343" />

            <!-- A workaround for gradient stroke: Adding a secondary overlay -->
            <!--            <solid android:color="@android:color/transparent"/>-->
        </shape>
    </item>
</selector>